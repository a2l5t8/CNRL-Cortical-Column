# Notebooks

The old top-to-bottom exploratory notebook was removed because it imported the
pre-manuscript column and non-spiking readout baselines. Use the fully spiking
scripts in `../scripts/` as the executable source of truth.
